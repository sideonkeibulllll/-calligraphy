import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.tiancat.calligraphy',
  appName: '归色临摹',
  webDir: 'dist',
  android: { backgroundColor: '#1a1714' }
}

export default config
